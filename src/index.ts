class Client {
  constructor(
    host?: null,
    port?: null,
    auth_token?: null,
    root_certs?: null,
    transaction_id?: null,
    tls?: null
  ) {
    
    console.log("Connected to Pachd at ");
  }
}

export default Client;
